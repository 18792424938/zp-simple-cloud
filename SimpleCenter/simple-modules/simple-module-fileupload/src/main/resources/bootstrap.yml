server:
  port: 9150

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://zp:123456@localhost:7000/eureka

spring:
  profiles:
    active: dev
  application:
    name: simple-module-fileupload
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
